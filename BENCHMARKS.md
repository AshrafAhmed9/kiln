# Benchmarks

One row per phase: what changed, tokens/s, TTFT, p99 ITL, memory, and (for
lossy changes) perplexity delta. Also tracks the ADR-010 code budget per
part (`csrc/` and `kiln_py/` counted separately), and (per ADR-009) the
exact free-tier hardware/session used for any GPU number, since no paid or
persistent CI exists to pin this automatically. Phase 4 adds the §6
boundary-cost measurement (Python-orchestration-overhead vs C++-executor
time per decode iteration) as its own tracked row.

| Phase | Change | tok/s | TTFT | p99 ITL | Memory | Perplexity Δ | Lines (csrc / kiln_py, non-test) | Hardware / seed |
|---|---|---|---|---|---|---|---|---|
| 0 | Foundations: arena allocator, pybind11 boundary (`ping`), CI, oracle | — | — | — | — | — | ~40 / ~10 | MacBook, N/A |
| 1 | Safetensors loader (mmap, zero-copy views) + byte-level BPE tokenizer | — | — | — | — | — | running total below | MacBook, N/A |
| 2 | CPU forward pass: GEMM, RMSNorm, RoPE, GQA attention, SwiGLU, full model | — | — | — | — | — | running total below | MacBook, N/A |
| 3 | Contiguous KV cache + seeded sampler (greedy/temp/top-k/top-p/rep-penalty) | — | — | — | — | — | running total below | MacBook, N/A |
| 4 | Padded static batching + **§6 boundary-cost measurement** (real number, not yet the production checkpoint): on a toy config (vocab 1000, hidden 64, 4 layers, CPU, `python3 -c` timing script) a bare Python→C++ round trip (`_C.ping()`) costs ~0.18µs; one real one-token decode step (KV-cached forward through all 4 layers) costs ~331.6µs. Python/pybind crossing overhead is **~0.06%** of one decode step here. This is a small-model CPU proxy for the plan's real claim, not the final measured number — that comes once a real checkpoint is served; recorded honestly as a directional result, not the finished benchmark. | — | — | — | — | — | **1,559 / — ** | MacBook, seed 1 |
| 5 | Continuous-batching scheduler (Orca-style admission/preemption), pure Python, tested against a mock executor with seeded random arrivals | — | — | — | — | — | — / running total below | MacBook, seeds 1-5 |
| 6 | OpenAI-compatible API (`/v1/completions`, SSE streaming), Part I assembly | — | — | — | — | — | **1,559 / 430** (of a 6–10k `csrc` and its own `kiln_py` budget — ADR-010) | MacBook, N/A |
| 7 | CUDA validation: the raw-CUDA attention, RMSNorm, greedy-argmax, and RoPE kernels compiled with NVCC 12.8 and passed 4 CPU-vs-GPU reference tests on a Kaggle Tesla P100 (sm_60); full CTest passed **57/57**. Triton RoPE also matched its CPU reference after pinning a PyTorch wheel with Pascal support. Nsight Compute was invoked but Kaggle denied GPU-counter access (`ERR_NVGPUCTRPERM`), so no profiler metric exists. This establishes buildability and small-input correctness only — no throughput/VRAM number or model integration. | n/a | n/a | n/a | n/a | n/a | 322 (`csrc/kernels/`) | Kaggle Tesla P100-PCIE-16GB, sm_60, CUDA 12.8 |
| 8 | Paged KV cache: block-table allocator with copy-on-write prefix sharing. CPU-only, fully tested (allocator property tests, copy-on-write correctness, paged-vs-contiguous parity). | — | — | — | — | — | running total below | MacBook, seed 99 |
| 9 | INT8 (per-channel) + INT4 (group-wise, packed) quantization, cross-checked against an independent Python reference through the pybind11 boundary. Real deliverable (WikiText-2 perplexity/KL) deferred — no real checkpoint/dataset offline; synthetic quantized-matmul MSE proxy used instead. | — | — | — | — | — | running total below | MacBook, seed 7 |
| 10 | Speculative decoding (rejection sampling). **Proven exact in greedy mode**: token-for-token identical to direct target-only greedy decoding, across 3 draft lengths. Random-sampling mode runs correctly but its exact distribution-preservation is not statistically proven in this session (see docs/defense.md). No KV cache (full recompute per round) — a named simplification, not a hidden one. | — | — | — | — | — | running total below | MacBook, seeds 1/2/5/6/9/10 |
| 11 | Parity-harness self-test: a deliberately broken RMSNorm (missing mean division) is proven to fail the project's real tolerance check. Nightly GPU perf-regression tracking remains impossible free (ADR-009), unchanged from Part I. | — | — | — | — | — | — | MacBook, N/A |
| 12 | Tensor-parallel sharding math (column-parallel, row-parallel, and the paired column→row pattern), CPU-simulated in numpy, proven exact for 1/2/4 simulated ranks. Proves the algorithm only — no real NCCL/multi-GPU communication was exercised (see docs/defense.md). | — | — | — | — | — | running total below | MacBook, seeds 1/2/3 |
| 13 | Evaluation infrastructure: exact-match task scoring, perplexity, bootstrap confidence intervals, a paired regression gate, canary-replay diffing. Tested against small, hand-controlled inputs (not the real, untrained model) — see docs/defense.md for why that's the more meaningful test here. | — | — | — | — | — | running total below | MacBook, seeds 1-5 |
| 14 | LoRA adapter merging is proven by hand computation and a real forward pass. `tools/train_lora.py` also performed a reproducible 20-step PEFT SmolLM2 `q_proj` fine-tune on the included four-record fixture and exported a 1.1 MB adapter manifest matching Kiln's `wq` merge contract. This proves training/export mechanics, not quality; licensed task data, evaluation, and multi-GPU DDP→FSDP remain open. | — | — | — | — | — | running total below | MacBook CPU, seed 20260824 |
| 15 | The scripted demo (`demo.sh`) — actually run end to end in this session, not just written: build, test, serve, curl (streaming + non-streaming), run the eval gate. Three writeups drafted from the phase learning notes (`docs/writeups/`). | — | — | — | — | — | — | MacBook, N/A |
| 16 | Multi-tenancy: hashed API keys, daily token quotas + per-second rate limits (enforced before, not after, generation), usage metering — a separate FastAPI service per constitution §6. Both of the plan's named DoD tests pass: tenant-isolation-under-hammering and a leaked-key revocation drill. In-memory store (not Postgres) — logic proven, persistence layer not yet built. | — | — | — | — | — | running total below | MacBook, N/A |
| 17 | The playground: a self-contained static HTML/JS page (no Next.js/build step — a deliberate scope call, Node was available; see docs/defense.md), live side-by-side temperature comparison with real measured latency, quickstarts in curl/Python/JS. Manually verified against a real running server. Quantization-level/spec-decode toggles not yet wired (no alternate servable configurations exist behind the API yet). | — | — | — | — | — | **2,213 / 1,156** (of the 6–10k `csrc` and its own `kiln_py` budget — ADR-010; `csrc` excludes the 322 unverified `csrc/kernels/` lines) | MacBook, N/A |
| 18 | Real Prometheus metrics (`/metrics`, wired to a real docker-compose Grafana/Prometheus stack) and a real, working Docker deployment — **actually built and run in this session**, catching two real bugs along the way (a missing `-fPIC` flag breaking the Linux build; a missing copy of installed console scripts breaking the container's entrypoint — both fixed, both re-verified). **No public deployment, no real users, no incidents, no retention data exist, and none are fabricated** — see docs/learning/phase-18.md. `docs/postmortems/TEMPLATE.md` is a template with no incident in it. | — | — | — | — | — | — | MacBook + Docker Desktop (linux/arm64), N/A |
| 21 | Seeded synthetic prefix-cache workload plus a local-process status page. The cache executable drives the real `PagedKVCache` through 80 conversations sharing a 16-token system prompt and four 20-token variants, each diverging in a partial shared block: **248 hits / 252 prefix-block lookups = 98.41%**, with 80 observed copy-on-write events. This is explicitly a synthetic mechanism measurement, not playground or production traffic. `/status` reads the already-wired Prometheus counters from the current process and labels their reset-on-restart, local-only scope. | — | — | — | — | — | running total below | MacBook, seed 20260824 |
| 22 | Hugging Face parity: `HuggingFaceTB/SmolLM2-135M-Instruct` (BF16 safetensors, loaded into Kiln as FP32) against Hugging Face FP32. The named 10-prompt fixture covers prose, numbers, punctuation, Unicode, and 8–16 token inputs: every top-1 token matched; worst final-logit difference was **7.44×10⁻⁵**. This remains final-logit evidence, not per-layer parity. Replacing the ASCII-only pre-tokenizer with ICU Unicode categories made the seeded tokenizer conformance fixture **10,000/10,000** exact matches (ADR-014). | — | — | — | — | — | running total below | MacBook, seed 20260824 |
| 23 | Cached decode batching: `Model::ForwardDecodeBatch` runs one decode token for each active sequence, sharing batched matrix work while retaining one KV cache and absolute position per sequence. A C++ parity test proves its two-sequence logits match independent cached decode. Both normal and SSE `/v1/completions` now use the scheduler worker; a concurrent-streaming API test covers the shared path, and an SSE disconnect cancels its queued/running request. New prompts still prefill individually. | — | — | — | — | — | running total below | MacBook, N/A |

Test code (exempt from the budget per ADR-010): 1,885 lines across `tests/cpp` and `tests/py` (49 C++ tests, 45 Python tests — 94 total, all passing, C++ under ASan/UBSan, mypy clean).

**Not yet measured, stated honestly:** real tokens/s, TTFT, and p99 ITL against a genuine trained checkpoint on real hardware. A real checkpoint has passed one CPU final-logit parity check, but it has not been served through GPU kernels. Phase 7's small-kernel correctness run is not a performance result: profiling, throughput, and the Triton path remain. Phase 9's real accuracy/latency/VRAM table, Phase 10's real decode speedup, and Phase 12's real scaling efficiency/communication overhead remain deferred to future Kaggle GPU time. Phase 14's real LoRA training/scaling study and Phase 18's real public launch require separate decisions and resources. CPU-only correctness and the P100 kernel check are evidence for their narrow claims, not substitutes for those remaining measurements.
