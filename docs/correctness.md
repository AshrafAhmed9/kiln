# Correctness — the "how do you know" document

This is the running catalog of what the parity harness checks, what it
can't, and every numerics bug it has caught. Each entry: what broke, how
parity caught it, the fix, and **what I misunderstood** — the misconception,
not just the bug — per the learning-loop rule (project memory
`kiln-learning-loop`).

No entries yet — Phase 0 has no numerics to get wrong.
